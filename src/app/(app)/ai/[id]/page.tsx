export const metadata = { title: 'Halo AI' };

/** Khung AI do `ai/layout.tsx` dựng — page chỉ giữ metadata để không remount khi đổi phiên. */
export default function AiSessionPage() {
  return null;
}
